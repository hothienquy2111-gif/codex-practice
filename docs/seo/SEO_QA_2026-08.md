# SEO V3 QA - 2026-08

## Automated Gates

| Check | Result |
|---|---|
| `node --check product-detail.js` | PASS |
| `node --check scripts/validate-seo.mjs` | PASS |
| `node scripts/validate-seo.mjs` | PASS - 14 public pages |
| `git diff --check` | PASS |
| Static title/description/canonical/robots/H1 | PASS via SEO validator |
| Inline JSON-LD parse | PASS via SEO validator |
| Local asset/link existence | PASS via SEO validator |

## Product Fixture QA

No production write or test product was created. Supabase was replaced by a browser-local read-only fixture.

Tested at `1440x900` and `390x844`:

- valid product A: index metadata, product canonical, Product/Breadcrumb schema, product H1;
- valid product B: same contract with independent metadata;
- missing ID: noindex, no canonical, no Product schema;
- malformed ID: noindex, no canonical, no Product schema;
- unknown ID: noindex, no canonical, no Product schema;
- network error: noindex, no canonical, no Product schema.

Result: **12/12 PASS**, with no page errors and no horizontal overflow.

## Honest Limit

Live product browser QA was blocked by the local network sandbox during the baseline run. The product lifecycle result above is fixture-based; it does not claim that live Supabase browser rendering was revalidated in this pass.

# SEO QA — Anh Minh Store

Ngày: 2026-07-30  
Nhánh: `seo/full-spectrum-seo-v2`  
Trạng thái tổng: **BLOCKED — còn 2 lỗi crawl files cần owner xác nhận lại**

## 1. Test matrix

| Nhóm | Cách kiểm tra | Kết quả |
|---|---|---|
| Git base | `main` so với `origin/main` | PASS — cùng `f912aad2...` trước khi tạo nhánh |
| Working branch | branch/status | PASS — `seo/full-spectrum-seo-v2` |
| JS syntax | Node `--check` cho 4 JS bị tác động | PASS |
| Diff whitespace | `git diff --check` | PASS |
| Forbidden paths | diff admin/Supabase/CNAME/workflow | PASS — không có thay đổi |
| Secret scan | kiểm tra added diff | PASS — không thêm key/token/secret |
| Static DOM | 14 trang qua Chrome | PASS |
| Canonical HTML | 14 trang | PASS — không còn GitHub Pages trong HTML |
| Noindex footprint | 404/compare/placeholder/filter/product default/duplicate service | PASS |
| JSON-LD parse | homepage + trang có helper | PASS |
| Product valid | stub active product | PASS |
| Product invalid id | id chứa markup | PASS |
| Responsive | 6 trang × 5 viewport | PASS — không horizontal overflow |
| Duplicate IDs | browser DOM | PASS |
| Breadcrumb | trang trọng yếu | PASS |
| Semantic address | trang liên hệ | PASS — 3 `<address>` |
| Carousel loading | resource/DOM check | PASS |
| `robots.txt` domain | validator | FAIL — repository gate |
| `sitemap.xml` domain/architecture | validator | FAIL — repository gate |

## 2. Product state assertions

### Sản phẩm active hợp lệ

Fixture:

- id: `sony-55x80l-55-inch`;
- brand/model/name/price/image đều được cung cấp;
- price: `12.990.000 ₫`.

Observed:

- title và H1 dùng đúng tên.
- canonical:
  `https://www.anhminhstore.io.vn/product-detail.html?id=sony-55x80l-55-inch`
- robots: `index,follow,max-image-preview:large`.
- OG URL khớp canonical.
- Product schema parse được.
- Offer price: `12990000`, currency `VND`.
- Không có availability, rating, aggregateRating hoặc review.
- Không horizontal overflow.

### ID sai

Fixture: id chứa `<script>`.

Observed:

- title/H1: không tìm thấy sản phẩm.
- canonical bị gỡ.
- robots: `noindex,follow`.
- Product schema count: 0.
- Không horizontal overflow.

### Lỗi tải dữ liệu

Code path dùng trạng thái “Dữ liệu sản phẩm đang được cập nhật”, noindex, bỏ canonical và xóa schema. Local browser baseline gặp CDN sandbox denial, vì vậy path này đã được thực thi thực tế.

## 3. Structured data assertions

- Homepage inline JSON-LD parse được.
- `seo-schema.js` tạo JSON-LD parse được trên các trang cấu hình.
- Service schema chỉ dùng `areaServed=Đà Nẵng`, không tự tạo địa điểm/geo.
- Organization không khai báo legalName/sameAs/openingHours chưa xác minh.
- Product Offer chỉ sinh khi giá hợp lệ.
- Không có fake review/rating/availability.

Rich Results Test/Schema Markup Validator live chưa chạy vì nhánh chưa deploy. Đây là owner action sau merge/deploy.

## 4. Responsive/browser

Viewport kiểm tra: 390, 430, 768, 1024, 1440 px.

Trang mẫu:

- homepage;
- liên hệ;
- sửa tivi;
- tivi cũ Đà Nẵng;
- sản phẩm khác;
- product default.

Kết quả:

- 0 horizontal overflow.
- 0 breadcrumb overflow.
- 0 duplicate id.
- H1 count hợp lệ.

Rủi ro còn lại:

- nhiều touch target nhỏ hơn 44×44 px từ baseline;
- floating contact có thể che nội dung/CTA trên màn hình thấp;
- chưa test screen reader và thiết bị thật.

## 5. Visual critique sau body/CSS changes

Đánh giá theo 12 chiều, không cộng thành điểm:

1. Brief/audience fit — **Adequate**: bố cục bán hàng sáng, rõ.
2. Message/action clarity — **Strong**: H1 và CTA chính dễ thấy.
3. Brand identity — **Adequate**: màu xanh/cam nhất quán; một số asset logo tải chậm.
4. Commercial hierarchy — **Strong** trên desktop; **Adequate** trên mobile.
5. Typography/legibility — **Adequate**: tiếng Việt hiển thị đúng; một số text phụ nhỏ.
6. Composition/spacing — **Adequate**: breadcrumb không phá nhịp; mobile contact card vẫn gọn.
7. Color/contrast — **Adequate** bằng quan sát; chưa đo toàn bộ cặp màu.
8. Depth/crop — **Adequate**: hero crop ổn; banner phải giữ asset hiện có.
9. Material/effects — **Unknown** ngoài asset hiện có; không tạo visual mới.
10. Motion/interaction — **Adequate**: carousel giữ thao tác, ảnh sau được hydrate trước khi chuyển.
11. Originality/anti-template — **Adequate**: giữ nguyên ngôn ngữ thiết kế hiện tại.
12. Production/responsive/accessibility — **Adequate**: không overflow; touch target/floating CTA còn backlog.

Kết luận thị giác: breadcrumb và semantic changes không tạo regression rõ trong screenshot desktop/mobile. Thiết kế không bị thay đổi đáng kể.

`TECHNICALLY COMPLETE — AWAITING QUÝ'S VISUAL APPROVAL`

## 6. Performance evidence

Homepage local, Chrome headless, cùng phương pháp đo:

- Baseline image transfer: 107,606,219 bytes.
- Sau sửa: 74,911,610 bytes.
- Chênh lệch: giảm 32,694,609 bytes (~30.4%).
- Chỉ 1/5 ảnh carousel có `src` ở 0,8 giây đầu.
- 4/5 ảnh chờ hydrate.

Không có Lighthouse score hoặc production CWV; không suy diễn điểm.

## 7. Validator hiện tại

Lệnh:

```powershell
& 'C:\Users\DELL\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' scripts/validate-seo.mjs
```

Kết quả:

```text
SEO validation FAIL (2 lỗi):
- robots.txt: vẫn công bố host GitHub Pages thay vì domain chính thức.
- sitemap.xml: vẫn công bố host GitHub Pages thay vì domain chính thức.
```

Hai lỗi này là blocker đã biết, không phải regression ẩn. Không được đổi thành PASS cho đến khi crawl files được owner xác nhận lại, sửa và parse/validate.

## 8. Kết luận QA

Source SEO, product lifecycle, structured data, breadcrumb, noindex và responsive đã qua test trong phạm vi local. Nhánh chưa đạt cổng cuối vì `robots.txt`/`sitemap.xml` vẫn sai host. Chưa push, chưa mở PR, chưa merge, chưa deploy.

